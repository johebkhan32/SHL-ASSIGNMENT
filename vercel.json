{
    "version": 2,
    "builds": [
      { "src": "api/api.py", "use": "@vercel/python" }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "api/api.py" }
    ],
    "ignoreCommand": "git diff --quiet HEAD^ HEAD ./",
    "git": {
      "deploymentEnabled": {
        "main": true
      }
    }
}

  