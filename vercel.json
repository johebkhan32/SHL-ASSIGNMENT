{
    "version": 2,
    "builds": [
      { "src": "app.py", "use": "@vercel/python" }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "app.py" }
    ],
    "ignoreCommand": "git diff --quiet HEAD^ HEAD ./",
    "git": {
      "deploymentEnabled": {
        "main": true
      }
    }
}

  